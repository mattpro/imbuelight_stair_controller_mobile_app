import 'package:flutter/material.dart';

class ConnectionTable extends StatelessWidget {
  const ConnectionTable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
